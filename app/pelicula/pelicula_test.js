'use strict';

describe('myApp.pelicula module', function() {

  beforeEach(module('myApp.pelicula'));

  describe('Pelicula Ctrl', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PeliculaCtrl = $controller('PeliculaCtrl');
      expect(PeliculaCtrl).toBeDefined();
    }));

  });
});
