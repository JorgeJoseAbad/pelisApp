'use strict';

describe('myApp.peliculas module', function() {

  beforeEach(module('myApp.peliculas'));

  describe('peliculas controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var peliculasCtrl = $controller('PeliculasCtrl');
      expect(PeliculasCtrl).toBeDefined();
    }));

  });
});
